# Build output — never commit binaries.
/target
/dist

# Regenerated by `cargo build -p embersong-wasm --target wasm32-unknown-unknown`.
*.wasm

# Vendored at build time from a system font (see crates/embersong-host/build.rs).
**/assets/fonts/body.ttf

# Local save file written by the game on every turn — personal progress.
embersong-save.json

# Editors and OS droppings.
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db
